# 本机验证记录

## 2026-09-22：v0.5.1 默认启用校正

新安装及省略配置项的旧安装默认开启 SenseVoice 校正，安装器自动准备对应模型；明确关闭与自定义目录仍保留。默认模型下载同时包含校正模型，安装器逐项下载时显式限定范围，避免绕过个人配置。149 项 Python 测试、5 项 C++ 测试和 16 项网页动效测试通过。使用全新 `build/release-v0.5.1` 目录执行 Release 构建。

配置与安装测试使用隔离目录和下载替身；本机已有模型会校验后复用。未进行全新虚拟机联网安装验收，未增加真实麦克风测试。

## 2026-09-22：v0.5.0 发布包

新增可选的流式预览加 SenseVoice 定稿校正。148 项 Python 测试、5 项 C++ 测试和 16 项网页动效测试通过；从全新 `build/release-v0.5.0` 目录执行 Release 构建。版本号、deb 下载入口、中英文 README 与宣传页同步为 0.5.0，校正功能默认关闭。

公开录音按真实语速回放、取消竞态与校正失败回退的验证见[实现记录](research/2026-09-22-streaming-refinement.md)，模型选择依据见[模型评测](research/2026-09-22-asr-model-evaluation.md)。实际 deb 已解包核对插件、服务源码与版本；包内服务通过 16 项校正测试和公开录音识别，包内插件通过流式与长按集成测试，apt 模拟安装依赖解析成功。未进行全新虚拟机联网安装验收或用户真实麦克风录音验收。

## 2026-09-18：v0.4.0 发布包

在全新 `build/release-v0.4.0` 目录执行 Release 构建，通过 127 项 Python 测试、5 项 C++ 测试和 12 项网页动效测试。deb、Python 服务、Fcitx5 附加组件和源码安装脚本的版本统一为 0.4.0。README、宣传页及安装就绪提示同步介绍长按 Ctrl+Alt，下载链接统一指向 v0.4.0。

实际 deb 解包后的原生插件通过长按录音集成测试；包内 Python 服务通过持续录音相关测试。逐文件核对包内插件与新构建一致，服务源码与工作区一致，确认不再沿用 v0.3.0 的原生插件。测试使用隔离上下文、合成音频和模型替身，未访问用户麦克风或修改本机正在运行的输入法，未进行全新虚拟机联网安装验收。

## Ctrl+Alt 长按录音（2026-09-18）

- Ctrl+Alt 按住约 300 毫秒启动；任一键松开结束；左右按键和先后顺序均覆盖。
- 隔离的真实 Fcitx5 上下文验证短按、其他 Ctrl+Alt 快捷键、重复按键、焦点切换、上下文销毁、密码框、已有预编辑、启动途中松键及尾句提交。修饰键的按下和松开均透传给应用。
- 真实 Unix socket 验证持续录音超过配置的时间限制仍保持采集，收到 stop 才结束；合成 31 秒 PCM 验证持续采集不截断。
- 离线识别覆盖长音频分段、静音分段跳过及不足 0.1 秒的尾部补零，保留原预览提交方式。
- 验证使用合成音频和模型替身；未进行真实键盘、麦克风及长录音识别质量验收。

## 2026-09-18：默认主题与安装包 0.3.0-3

安装包包含系统与用户初始化所需主题资源；首次配置启用 LiltKey 浅色主题，已有自定义主题、深色偏好和非默认字体保留。一次性初始化记录保证升级不重置后来的选择。原配置在迁移前备份，卸载后保留用户主题和外观设置。

119 项 Python 测试、4 项 C++ 测试、10 项网页动效测试通过；测试包括从解包后的 deb 运行真实用户安装（临时 HOME/XDG、复用测试 Python 环境），验证主题文件和默认配置。实际主题的 65 个字段已通过 Fcitx5 5.1.19 原生解析器读取核对，PNG 使用 GdkPixbuf 加载验证。

README 图像由隔离 Xvfb/X11、xcompmgr、GTK3 和 Fcitx5 实际渲染后直接截图，没有访问真实麦克风或修改日常桌面的输入法服务。完整初装的联网下载与系统命令仍由测试替身覆盖，未做全新虚拟机联网安装验收。

## 2026-09-11：仅安装 deb 自动配置

`fcitx5-voice_0.3.0-2_amd64.deb` 增加安装/卸载钩子、系统级 XDG 登录自启动入口和用户级自动配置服务。安装钩子仅向已登录的图形用户管理器投递任务，实际配置以普通用户权限完成；无桌面会话时由下次登录触发。失败后每 5 分钟重试，成功记录安装代次，重复登录不重复初始化，同版本重装仍重新检查。

验证结果：112 项 Python 测试通过，包括 deb 内容及脚本入口、成功/失败/重试、并发互斥、同版本重装、桌面环境导入、用户权限分派、升级与移除、通知不可用时的容错。桌面入口通过 `desktop-file-validate`，自动配置服务通过 `systemd-analyze --user verify`，维护脚本通过 `sh -n`。Fcitx5 重启由独立的 `Type=forking` 瞬态用户服务托管，避免随配置 oneshot 结束而被终止。

系统命令与下载步骤在测试中使用替身，真实用户文件操作和 socket 握手使用临时目录验证；未在本机实际安装 deb、切换输入法或访问麦克风，未做全新虚拟机安装验收。模型与 Python 依赖在安装后的用户桌面任务中联网下载，因此收到就绪通知后才能使用；首次切换输入法需要注销重新登录。

## 2026-09-11：一条命令安装

新增 `bash install.sh`，串联系统依赖、原生插件构建、用户运行环境、按个人配置选择的模型、诊断、服务自启动、就绪握手和 Fcitx5 默认输入法配置。`--deb` 使用本地安装包跳过编译；包内的 `fcitx5-voice-setup` 自动完成用户配置。Debian 包修订号为 `0.3.0-1`，可从原有 `0.3.0` 升级。

| 检查 | 结果 |
|---|---|
| Python 自动测试 | 99 项通过 |
| 新增安装流程覆盖 | 首次默认配置、重复安装、保留离线配置与自定义目录、关闭标点、deb 路径、依赖/下载/服务失败中断、root/非桌面会话拒绝 |
| 真实 Unix socket 就绪检测 | 收到 ready 后退出，不发送录音指令；无服务时超时失败 |
| 插件升级 | 原子替换文件；保持运行进程持有的旧 inode 内容不变 |
| C++ 构建与 CTest | 构建成功，4/4 通过 |
| apt 依赖解析 | Ubuntu 26.04 本机 `apt-get --simulate install` 成功 |
| Debian 包 | 构建成功；解包验证原生插件、服务源码、自动配置入口及入口帮助命令 |
| Shell 和差异检查 | `sh -n install.sh`、`git diff --check` 通过 |

安装测试在临时 HOME/XDG 目录中执行真实用户文件安装，系统包管理、编译编排、模型网络下载及桌面命令通过测试替身验证调用和失败路径；模型下载器沿用原有校验测试。实际 C++ 构建使用现有开发环境。未在全新虚拟机执行联网安装，未修改本机默认输入法或运行中的用户服务，未访问麦克风。首次切换输入法仍需注销并重新登录。

## 2026-09-10：0.3.0 定稿标点与空格

沿用下述 CPU 环境，sherpa-onnx 1.13.7、4 线程。新增本地 CT-Transformer 中英 int8 标点模型，文件 75,519,198 字节，SHA256 为 `65a3fb9f5ad7bfb96bf69e0dc4481df97f6ee60513c1d94ce981ba6effd524b1`。下载清单同时固定官方归档与模型文件摘要，校验后原子写入。标点模型只处理 final，partial 保持即时显示。

| 检查 | 本次结果 |
|---|---|
| Python 自动测试 | 87 项通过，含定稿处理、取消、失败回退、数值/URL 保护与模型下载校验 |
| C++ Release 构建及 CTest | 构建成功，4/4 通过 |
| 标点模型加载 | 125.64 毫秒；另一次测量 137.8 毫秒、增加约 131.4 MiB RSS |
| 五组短句，各 10 次 | 标点和空格处理耗时中位数 0.92～1.98 毫秒 |
| 公开中文样本实时 Socket 回放 | 14 次 partial、2 次 final；每次 final 均为“开放时间，早上九点至下午五点。” |
| 上述回放延迟 | 首个 partial 1.624 秒；两次定稿处理 5.952 / 2.270 毫秒；stop 到 finished 0.033 秒 |
| 用户安装 | 已安装 0.3.0；服务 enabled/active；实际 Socket 返回 protocol 2；安装源码与工作区一致 |

回放使用下文相同的 5.592 秒公开中文样本，追加 2 秒静音后重复样本。首句在 stop 前提交，尾句在 stop 后排空提交，没有重复 final，partial 没有被提前加标点。未访问用户麦克风。原生插件二进制未变化，没有重启 Fcitx5；语音服务已重启，保留原有 `MicYouVirtualMic` 和配置文件全部字节。升级备份位于用户数据目录 `fcitx5-voice/backups/20260910-201336-punctuation`。

实际文本验证：“今天天气很好我们一起出去散步吧” → “今天天气很好，我们一起出去散步吧。”；“你今天下午有时间吗我们一起讨论一下这个方案” → “你今天下午有时间吗？我们一起讨论一下这个方案。”；“使用Linux系统和Python3开发下午3点开会” → “使用 Linux 系统和 Python3 开发，下午3点开会。”。

模型可能错误插入标点或使用中文全角标点处理英文。每次只看当前 ASR 分句，不回改已提交正文。模型若改词、破坏原有数值/缩略词/URL，或运行失败，保留原文并做空格整理。中文 URL 域名及路径按完整 token 保留，URL 后接正文应有空白或句读分界。极端 8,191 字节重复输入测得约 1.061 秒，输出超过限制后回退；短句测量不是长文本延迟上界，也不是 P95 或用户麦克风验收。

## 2026-09-10：0.2.0 流式输入

本次沿用下述本机环境，使用 sherpa-onnx 1.13.7、CPU 4 线程。未访问用户麦克风，也未修改正在运行的用户服务或重启桌面输入法。

流式模型为 `csukuangfj/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20`，固定 revision `98590b7ed6443e77b714204da2757d75e1a642f4`。四个文件约 190 MiB，SHA256 均已通过校验；具体摘要在 `src/fcitx5_voice/model_manifest.py`。流式模型自动识别中英文，配置要求 `language="auto"`。

| 检查 | 本次结果 |
|---|---|
| Python 自动测试 | 58 项通过，包括流式会话、GStreamer、真实 Socket、下载校验和离线兼容 |
| C++ Release 构建及 CTest | 构建成功，4/4 通过；包含两套隔离的真实 Fcitx5 上下文测试 |
| 公开中英样本 `test_wavs/0.wav`，10.053 秒 | 模型加载 0.620～0.681 秒；完整解码 0.253～0.270 秒；产生 17 次 partial、1 次端点 final 和 1 次停止尾句 final |
| 公开中文样本通过实际模型与 Unix Socket 实时回放 | 5.592 秒样本 + 2 秒静音 + 同样样本，合计 13.184 秒；14 次 partial、2 次 final，无重复分句提交 |
| 上述实时回放首个 partial | 开始回放后 1.620 秒，包含样本前导静音与模型音频上下文等待 |
| 上述回放 stop 到 finished | 0.025 秒；首句在停止前已经 final，末句在停止后排空完成 |

中文样本 SHA256 为 `b77f1794fe374a0ba1ee1dc458bfaf9349496cbbfc32780c50ba3c5a7ad8e373`，两次均识别为“开放时间早上九点至下午五点”。该样本来自下文 SenseVoice 上游的公开 `test_wavs/zh.wav`。中英样本 SHA256 为 `7d93384ca14702cc584a7a33fe2fed92e89e708549161cb12ea38c916882103b`，输出为 `昨天是 MONDAY` 和 `TODAY IS LIBR THE DAY AFTER TOMORROW是星期三`，其中存在明显的英文识别错误。两个样本不能代表用户口述准确率；本次没有声称 CER 或 P95 达标。

新增验证覆盖：录音中预编辑完整替换；无客户端 Preedit 能力时回退面板；分句定稿清空预编辑后提交；空 final；过期 id/segment/seq 和重复 final 抑制；停止排空；取消与断线释放采集并抑制迟到结果；队列溢出和自然 EOF 报错；英文分句词边界；UI 回调与 commit 回调内 reset 时不继续访问过期上下文。离线 protocol 1、Enter 预览提交与原有测试继续保留。

桌面验收仍需使用真实应用和麦克风：浏览器、终端、编辑器的 preedit 显示与同一输入框内鼠标移动行为依赖客户端协议。当前验证证明了本地流式识别、服务协议与真实 Fcitx5 上下文分层联通，不等于所有桌面应用均已完成实机口述验收。

## 2026-09-09：0.1.0 离线基线

日期：2026-09-09。环境：Ubuntu 26.04、GNOME Wayland、Fcitx5 5.1.19；Intel Core i7-14700K、约 64 GB 内存。Python 3.11.15、sherpa-onnx 1.13.7，CPU 4 线程。

## 真实模型与音频

上游仓库：`csukuangfj/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17`，revision `2365baeacb507f821a0c8120fcee3d484dba7a07`。

量化模型 SHA256：`c71f0ce00bec95b07744e116345e33d8cbbe08cef896382cf907bf4b51a2cd51`。tokens SHA256：`f449eb28dc567533d7fa59be34e2abca8784f771850c78a47fb731a31429a1dc`。

公开样本 `test_wavs/zh.wav`，16 kHz PCM16，时长 5.592 秒。未访问用户麦克风。

| 测量 | 本次结果 |
|---|---|
| 量化模型加载 | 0.486 秒 |
| 量化模型连续三次推理 | 0.176 / 0.107 / 0.122 秒 |
| FP32 模型连续三次推理 | 0.130 / 0.121 / 0.120 秒 |
| GStreamer 文件输入 → socket 停止请求 → 真实模型最终结果 | 0.196 秒 |

以上只有单一公开样本，属于开发时测量，不是 P95 延迟、准确率验收或麦克风端到端延迟。CPU 调度、线程数、音频长度会影响结果。

实际转写为“开饭时间早上9点至下午5点。”；上游文档参考为“开放时间早上9点至下午5点。”。两个精度版本在此样本上有同一错字，不能据此宣称高准确率。保留量化模型作为可运行基线；下一步应使用用户真实录音集评估 CER、专有词和中英混输，再决定模型替换。**本版本的准确率目标尚未验收。**

## 服务验证范围

Python 自动测试 30 项通过（含用户级安装／迁移卸载、Debian 包内容）。独立审查发现的断线投递清理、深层 JSON 和错误 id 关联问题均已通过回归测试。用户服务已验证启动、ready 握手、正常退出与 socket 清理。

- 单会话开始、停止、结果投递；错 id 不停止合法录音。
- 录音自动上限、取消时释放采集进程、空音频和静音拒绝。
- 推理期间取消抑制迟到结果；结果投递时断线不破坏清理。
- 真实 Unix Socket 收发、0700 目录／0600 socket、单客户端独占。
- 超长消息、非法 UTF-8、过深 JSON、未知命令、重复服务和已有普通文件处理。
- GStreamer 合成音源完成真实子进程与 PCM 管道测试，不使用麦克风。
- 配置字段与资源限制验证。

## Fcitx5 集成验证

Release 构建通过，CTest 3/3 通过。隔离的真实 Fcitx5 实例加载绝对路径插件，并验证快捷键框架规范化、录音／停止、候选预览、UTF-8 末字删除、Enter 实际提交、失焦取消、普通键盘输入取消后透传、密码框／已有预编辑拒绝，以及启动中取消后销毁输入上下文再建立新会话。识别后端在此测试中是 Unix Socket fixture，未访问麦克风。

插件与服务已用户级安装，模型 SHA256 校验通过，systemd 用户服务已启用。`.deb` 已生成并检查包结构，不包含模型及第三方 Python 依赖。

## 桌面验收边界

自动测试不能代替真实桌面验收。需由使用者按快捷键在浏览器、终端、编辑器等应用中口述，并确认光标位置、预览、取消、窗口切换和麦克风选择。尚未对这些应用逐一进行真实麦克风测试。
