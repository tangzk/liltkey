# Final punctuation and spacing

User approved: keep streaming partials immediate, restore punctuation and normalize spacing before each final commit. Existing committed text is immutable. Update installed user service and push the existing .160 development branch after verification.

- [x] Add a local CT-Transformer punctuation adapter, pinned model manifest/download, meaningful tests and actual CPU measurements. Adapter callable takes one final segment and returns punctuated text. It must preserve spoken content, skip empty input, and reject excessive output. Defaults: punctuation=true; punctuation_model_dir under XDG data directory. Offline SenseVoice behavior remains unchanged.
- [x] Inject a final-only processor into StreamingSession/VoiceServer and CLI. Run punctuation in the existing inference worker outside the session lock. Check cancellation before/after processing. Runtime punctuation failure falls back to normalized raw text without losing dictation; never emit speculative punctuation in partials.
- [x] Add final spacing rules: collapse duplicate spaces, keep CJK adjacent, normalize punctuation spacing without breaking decimals/contractions, add space at CJK/ASCII letter boundaries (e.g. 使用 Linux 系统). Preserve legacy cross-segment English spacing and punctuation-free mode. Never insert control keys/newlines. Test socket final punctuation, stop tail, no double processing, disabled mode, error fallback and cancellation during slow punctuation.
- [x] Run Python and C++ suites, actual model text and public WAV service replay, independent review, docs and 0.3.0 packaging. Back up/update the running user service without touching microphone selection or restarting Fcitx5 unnecessarily. Verify installed source/version, model readiness and protocol2 handshake; commit/push and verify remote SHA.

Interfaces: `PunctuationRestorer(config)(text: str) -> str` in punctuation.py; final processor injected as `finalize=None` (identity behavior in default server/session tests). `finalize_text(text, punctuate=None)` in text.py applies optional restoration then final spacing/limit checks. Live calls through asyncio.to_thread; CLI uses same processor synchronously. Additive configuration preserves current 30-second session and hotkeys. Punctuation inference gets a bounded segment; model loaded once at service startup, never during recording.
